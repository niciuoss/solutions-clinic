# docker/compose.yml
services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: clinic
      POSTGRES_USER: clinic
      POSTGRES_PASSWORD: clinic
    ports: ["5432:5432"]
  redis:
    image: redis:7
    ports: ["6379:6379"]
